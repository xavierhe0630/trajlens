numpy>=1.24
pandas>=2.0
matplotlib>=3.7
pillow>=9.5

# Optional, only needed if you want to run the real pytest suite instead of
# tests/run_tests.py (which has no dependencies beyond the above):
# pytest>=7.0
